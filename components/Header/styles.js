import styled from 'styled-components'

export const Container = styled.header`
  align-items: center;
  background: #FFFFFF;
  display: flex;
  justify-content: center;
  padding: 1.5rem;
  position: sticky;
  top: 0;
  z-index: 99;

  .hamburguer-icon {
    position: absolute;
    left: 1rem;
  }
`